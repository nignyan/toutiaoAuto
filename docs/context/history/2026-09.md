# History 归档

> 完成事项按月归档，只留短摘要；完整细节保留在源码与设计文档中。

## 2026-09
- 初始化 Git 仓库（`2c813ee`）。
- 合并个性化开发规范至 AGENTS.md（`801601b`）。
- 创建 `PROJECT_INDEX.md` 与 `docs/context/` 分层结构（`5faf30d`）。
- 搭建 MVP 代码骨架：FastAPI + SQLite + pydantic 领域模型 + 内容形态决策（§5），ruff 通过、12 个测试通过。
- 运营工作台交互 Demo 重做并迁移至 `demo/`（`30ec584`、`7233dca`、`02ff63e`）。
- 产品设计按 demo 评审结论更新，新增系统设计文档（`9fe06f6`）。
- 头条草稿箱发布适配器 + CLI + 10 项单测，auto_publish 默认关闭（`07da83b`）。
- 修复 PROJECT_INDEX/MAP 文档漂移，索引同步至代码现状（`e610dd3`）。
- 拍板 D6 采集数据源 / D7 聚类口径 / D8 素材密度边界，新增设计规格（`03335b6`）。
- 采集→聚类→素材入库分 4 批落地：DAO/采集双源/聚类/素材入库重评估 + 4 API 端点（`4d72048`…`92ee093`）；微博 403 修复（`e39a2f6`）；上下文回填（`ae3ef62`）。pytest 71 例。
- 拍板 D9 内容生产引擎口径，新增设计规格（`eeabcfe`）。
- 内容生产引擎分批落地：Production 三态字段 + production 表/DAO（`989aef8`）；ContentComposer 协议 + TemplateComposer（`eb378e6`）；一票否决 + 质量分纯函数（`3206042`）；生产编排 + 3 API 端点（`88cd12c`）。pytest 121 例、ruff 零违规、demo 冒烟通过。
- 拍板 D10 账号分配与待发布队列入队口径，新增设计规格（`c3793a4`）。
- 账号分配 + 发布队列分批落地：账号/队列表与 DAO + role/vertical 模型扩展 + 存量库补列迁移（`4bf5bc7`、`95c6fb3`）；分配编排 allocator：资格三过滤 + 三层级 + 当日配额 + 批量容错（`9f0538f`）；账号 CRUD + enqueue/publish-queue 7 端点（`0829869`）。pytest 161 例、ruff 零违规、demo 冒烟通过。
- 素材等待队列超时归档（D11）落地：Event 增加 deferred_at/deferred_retries + 存量库补列；wait_queue 超时判定纯函数 + 事务批量归档；DEFERRED 补录计重试、ARCHIVED 不复活；`POST /pipeline/wait-queue/timeout`（`612ffef`）。pytest 175 例、ruff 零违规、demo 冒烟通过。
- 内容生产引擎复审收口（D12，grill 拍板，后端零代码改动）：HELD 降级「留档 + 人工捞回」（产品 §4.5「次日重试」列演进项）；一次性生产、署名素材级从严、质量分均值公式确认 MVP 口径，局限与「均值 vs 高密度」语义错位记 RISKS.md；多版本/重复检测/风险标签/标题脚本一致性/LLM composer 显式标记暂不做；demo 质检文案同步（`eef537b`）。demo 冒烟 14/14。
- 拍板 D13 发布执行口径（状态机新增 draft_ready + 队列操作全量对齐 §4.6），新增设计规格（`2485eaa`）。
- 发布执行分批落地：队列 draft_ready 状态 + sort_key 字段与 DAO 扩展/存量库补列（`2485eaa`）；dispatcher 编排：build_package 图文内容包 + 派发结果容错落库 + skip/unskip/confirm（`8622b0d`）；发布执行 API 6 端点 + adapter 依赖注入 + 删除守卫扩展，修复事务内二次抢锁死锁（`a50189a`）；demo 两段式发布同步 + 上下文文档回填。pytest 211 例、ruff 零违规、demo 冒烟 14/14。