# 热点内容自动化工作流｜产品设计文档

## 1. 产品定位

一套“热点素材驱动”的内容生产系统：全天发现全网热点，提取事件关联的视频或图片素材，自动完成选题、内容编排、短视频/图文制作、质量检查与发布排期。运营者只需在今日头条侧审阅草稿并点击发布。

本产品不生成 AI 画面；视频和图片均来自热点事件对应的原始媒体资产或已接入素材源。AI 的职责是发现、理解、筛选、编排和包装内容。

## 2. 目标用户与核心价值

面向需要持续运营头条内容账号的个人或小团队。

- 减少人工盯热点、找素材、写脚本、剪辑和排期的重复劳动。
- 将零散热点信号聚合为可生产的“事件”，提高内容时效。
- 根据热点素材形态自动选择视频、图文或图集视频的生产方式。
- 为每个账号提供当日可发布的内容队列，运营者仅处理最终发布动作。

## 3. 业务流程

```text
多源热点采集
  → 事件聚类与热度评分
  → 媒体素材提取与资产入库
  → 选择内容形态与账号角度
  → 自动生成脚本、标题、字幕、封面与成品
  → 自动质检与去重
  → 进入账号待发布队列 / 草稿工作台
  → 人工确认发布
  → 回收数据并优化选题策略
```

## 4. 主要功能

### 4.1 热点雷达

- 定时接入 X、抖音热榜、新闻/RSS 和垂直信息源。
- 记录热度增速、发布时间、互动信号、跨平台传播情况与原始链接。
- 按时效性、热度、账号垂类匹配度和素材完整度形成热点候选池。

### 4.2 事件聚类与选题中心

- 合并同一事件的多条帖子、视频和报道，形成统一事件卡片。
- 将事件与来源、时间线、关键人物、媒体资产关联。
- 为事件评分并自动挑选当日优先生产的选题。

### 4.3 媒体资产池

- 按事件归档视频、图片、音频、文字摘要及来源信息。
- 自动识别素材类型、时长、画幅、清晰度、字幕与主要画面。
- 计算**信息密度**并落库，供第 5 节形态决策使用：
  - **定义**：单位时长内「含明确主体且画面稳定」的镜头占比 × 关键信息要素出现次数的归一化值
  - **计算方与时点**：算法侧，在素材入库时计算并落库
  - **取值范围与分档**：0–100；≥ 70 为高（可作成片主体），50–69 为中（仅可作辅助素材），< 50 为低（不可作为视频主体）
  - **降级方案**：若模型打分稳定性不足，采用「模型打分 + 运营抽检校准」而非纯自动
- 对相同或高度相似素材生成指纹，避免重复使用。
- 标注素材的**来源分级与授权状态**，规则见 `素材授权与来源合规规则.md`：
  - `sourceType`（A 可直接使用 / B 限定条件使用 / C 禁止使用）、`authStatus`（cleared / pending / blocked）
  - `authEvidence`（凭证或原始发布链接）、`licenseScope`（是否可商用）、`attribution`（署名文本）
  - 只有 `authStatus = cleared` 的素材可进入生产；**C 类素材视同「无有效媒体」参与第 5 节形态判定**
  - **素材不足时降级形态，不得降级合规标准**

### 4.4 内容生产引擎

- **视频热点**：选择关键片段，自动裁剪为竖版，生成字幕、解说、标题条、封面与成片。
- **图片热点**：将多张图片编排为竖版图集视频，加入标题、序号、时间线、转场与旁白。
- **图文内容**：生成标题、事件摘要、时间线、背景说明与配图顺序。
- 同一事件可依据账号定位输出快讯版、解读版或垂直领域版。
- **暂缓生产与素材等待队列**：判定为「暂缓生产」的事件进入素材等待队列，持续尝试获取后续素材。
  - **等待窗口**：当日多次重试；跨日保留 24 小时
  - **状态定义**：等待中 / 已重试 n 次 / 已归档
  - **超时处理**：超过 24 小时仍未凑齐有效素材则归档，不再尝试（热点时效已失效，继续保留只会让等待队列膨胀）

### 4.5 自动质检

- 检查事件来源完整性、素材可用性、**素材授权合规性**、标题与脚本一致性。
- 检测重复事件、相似标题、相似脚本和重复镜头。
- 为特殊事件打上风险标签并转入待确认队列。
- 不满足质量阈值的产出（**质量分 < 75**）不进入当日发布配额，处理方式为**留档 + 降级次日重试，不自动丢弃**，运营可从留档中人工捞回。
- 硬性一票否决项（事件来源缺失、素材不可用、**素材授权未确认**、引用比例超限、署名缺失）直接拦截，质量分仅作分层依据。

### 4.6 账号与发布工作台

- 配置账号定位、内容偏好、日产量和发布时间窗口。
- 将成品按账号分配，生成每日待发布队列。
- 运营者可预览视频/图文、替换标题、跳过或调整排序。
- 支持记录发布状态、失败原因、审核状态与素材来源。

### 4.7 数据回流

- 汇总曝光、播放、完播、互动、负反馈和审核结果。
- 分析热点类别、内容形态、标题和发布时间表现。
- 反哺热点评分、账号选题偏好和内容模板排序。**MVP 阶段仅输出「建议动作」，由运营人工确认，不自动调整权重**；自动调参列入第 7 节后续演进。

## 5. 内容形态决策

### 5.1 判定优先级

事件素材按「素材可用性从强到弱」判定，**自上而下、命中即止，不再往下匹配**：

```text
规则 4（无有效媒体）→ 规则 1（有清晰且信息密度高的视频）→ 规则 3（视频与图片兼有）→ 规则 2（有多张图片）
```

即：先看有没有媒体，再看视频能否单独立片，再看能否视频 + 图片混编，最后退到纯图集。

### 5.2 规则明细

| 规则 | 事件素材情况 | 命中条件 | 优先产物 |
| --- | --- | --- | --- |
| 规则 1 | 有清晰且信息密度高的视频 | 存在「清晰度 ≥ 清晰」且「信息密度 ≥ 70」的视频。**图片不参与本规则判断** | 30–60 秒竖版热点视频 |
| 规则 3 | 视频与图片兼有，但视频质量不足单独立片 | 存在视频且存在图片；视频满足「清晰度 ≥ 清晰」**或**「信息密度 ≥ 50」二者之一 | 视频作引子片段 + 图片承担背景与时间线 |
| 规则 2 | 有多张现场图片/新闻图片 | 无可用视频，且图片 ≥ 2 张 | 图集视频或图文资讯 |
| 规则 4 | 仅有文字、无有效媒体 | 无视频，且图片 < 2 张 | 暂缓生产，进入素材等待队列 |

### 5.3 口径说明

- **信息密度**：取值范围 0–100，由算法侧在素材入库时计算并落库（对应 4.3）。定义见 4.3。
  - **≥ 70**：高，可作为成片**主体** —— 规则 1 的准入线
  - **50–69**：中，仅可作**辅助素材** —— 规则 3 的准入线
  - **< 50**：低，不可作为视频主体
- **清晰度分级**：复用 4.3 的素材属性识别结果，「清晰度 ≥ 清晰」指取值为「清晰」或「超清」。
- **视频质量不足时的退化路径**：优先退到规则 3（视频作引子 + 图片补充），而不是直接暂缓 —— 保住时效、不浪费事件；只有**既不达可用下限、图片又不足 2 张**时，才落回规则 4 暂缓。

> 本节口径已于 2026-09-15 MVP 演示评审拍板，决议记录见 `评审决议记录_内容生产口径确认.md`。

## 6. 首期范围（MVP）

- 聚焦一个垂直领域和一个头条账号。
- 每日采集 100–300 个热点信号，聚类为 30–50 个事件。
- 自动筛选带有效媒体素材的事件，产出 8–12 条待发布内容（**8–12 为软区间，12 为硬上限**）。
  - **排序**：按事件评分降序（热度 + 垂类匹配度 + 素材完整度 + 时效性）
  - **形态保底**：图集视频与图文类不少于 2 条，避免内容结构过度集中、素材过快耗尽
  - **低于 8 条**：记录原因（素材不足 / 质检拦截），供次日复盘，不允许静默发生
- 支持视频、图片图集视频和图文三种成品形态。
- 通过自有待发布工作台完成最终人工发布确认。

## 7. 后续演进

- 扩展多账号内容编排和差异化模板。
- 提升素材理解、事件发展追踪和热点预测能力。
- 建立素材授权/来源、质量评分和账号画像的统一后台。
- 数据回流闭环从「辅助决策」升级为「自动调参」：单次调幅 ≤ 15%、观察期 7 天、支持一键回滚，且仅对当前账号生效。
- 验证与平台发布能力的稳定接入，减少最终发布步骤的操作成本。
