# 热点内容自动化工作流｜系统设计文档

> 配套文档：`热点内容自动化工作流_产品设计.md`（产品口径）、`AGENTS.md`（开发规范）、`docs/context/DECISIONS.md`（决策记录 D1–D9）。
> 本文描述当前代码的真实结构与设计取舍，与实现保持同步；冲突时以代码为准。
> 最后核验：2026-09-16（内容生产引擎迭代落地后回填）。

## 1. 架构总览

系统分为四层：**交互层（Demo 工作台）→ 应用层（FastAPI）→ 领域层（pipeline / publish）→ 基础设施层（SQLite / Playwright / 文件系统）**。

```text
┌────────────────────────────────────────────────────┐
│ demo/ 运营工作台（纯前端，模拟数据 + 真实交互）        │
└──────────────────────┬─────────────────────────────┘
                       │（未来替换为真实 API 调用）
┌──────────────────────▼─────────────────────────────┐
│ app/api + app/main.py（FastAPI lifespan 注入 SQLite）│
│  /health + /pipeline/collect + /events(+{id}/assets) │
│  + /pipeline/produce(+all) + /productions            │
├────────────────────────────────────────────────────┤
│ 领域层                                              │
│  app/pipeline/   采集(collector)/聚类(clustering)/    │
│                  素材入库(asset_ingest)/形态决策       │
│                  生产(composer+producer)/质检          │
│                  (quality_check)                     │
│  app/publish/    发布适配层：PublishAdapter 协议      │
│                  └ ToutiaoDraftAdapter（RPA 草稿箱）  │
│  app/models/     领域模型：事件(含timeline)/素材/…     │
├────────────────────────────────────────────────────┤
│ 基础设施                                            │
│  app/daos/       SQLite 持久化（单连接+锁，幂等建表）  │
│  httpx           微博热搜公开 JSON（浏览器头，见 D6）  │
│  Playwright      浏览器持久化 profile（按账号隔离）    │
└────────────────────────────────────────────────────┘
```

## 2. 技术选型（对应 DECISIONS D4 / D5）

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 语言/框架 | Python 3.13 + FastAPI | 用户主力技术栈；API 与异步生态成熟 |
| 数据校验 | Pydantic v2 | 领域模型与内容包声明式校验 |
| 存储 | SQLite（单文件） | MVP 零运维；后续可平滑迁 PostgreSQL |
| 发布通道 | Playwright（可选依赖 `rpa` 组） | 头条号无个人发布 API，RPA 是唯一可行路径 |
| 测试 | pytest + ruff | 用户既定工具链 |
| Demo | 纯前端（无构建），localStorage 持久化 | 免安装直接打开；交互真实、数据模拟 |

## 3. 模块与代码映射

| 产品模块（产品设计文档 §4） | 代码位置 | 状态 |
| --- | --- | --- |
| 热点雷达 | `app/pipeline/collector.py`（SignalSource 协议 + 微博热搜 + Fixture） | **已实现 + 测试** |
| 事件聚类与选题中心 | `app/pipeline/clustering.py`（bigram Jaccard ≥ 0.35，D7） | **已实现 + 测试** |
| 媒体资产池 | `app/models/media_asset.py` + `app/pipeline/asset_ingest.py`（入库 + 重评估） | **已实现 + 测试** |
| 内容形态决策 | `app/pipeline/format_decision.py`（规则 4→1→3→2，命中即止） | **已实现 + 测试** |
| 内容生产引擎 | `app/pipeline/composer.py`（ContentComposer 协议 + TemplateComposer）+ `app/pipeline/producer.py`（编排 + 事务落库） | **已实现 + 测试** |
| 自动质检 | `app/pipeline/quality_check.py`（一票否决 + 质量分纯函数）+ `app/models/production.py`（三态 QualityStatus） | **已实现 + 测试** |
| 账号矩阵 | `app/models/account.py`（垂类/配额/窗口/域/发布配置） | **已实现 + 测试** |
| 发布通道 | `app/publish/`（协议 + 头条草稿箱适配器 + CLI） | **已实现 + 测试** |
| 数据回流 | `app/models/publish_queue.py`（发布状态记录） | 模型就绪 |
| 运营工作台 | `demo/`（8 视图 + 模拟引擎） | **已实现（模拟数据）** |

采集/聚类/素材入库的规格见 `docs/specs/2026-09-15_热点采集聚类素材入库_设计文档.md`（D6–D8）；
内容生产引擎规格见 `docs/specs/2026-09-16_内容生产引擎_设计文档.md`（D9）。

## 4. 核心数据模型

```text
Account ──────────────┐
  channel/adapter/auto_publish/profile_dir   ← 发布绑定
  vertical/daily_quota/publish_window        ← 自动分配依据

Event ──1:N── MediaAsset
  kw/vertical/score/status    type/clarity/density/dur
  timeline[]                  srcLevel(A/B/C)/auth(cleared/pending/blocked)
                              attribution/fingerprint

Production ──1:1── QualityResult
  type/rule/title             score/checks[]/vetoes[]
  body/cover/composer         status(qualified/held/blocked)

PublishQueueItem
  production_id/account_id/status(pending/published/skipped)/order
```

关键不变量：

- 仅 `auth = cleared` 且 `srcLevel ≠ C` 的素材参与形态判定与生产（D2）。
- 形态判定自上而下命中即止：规则 4（无有效媒体）→ 1 → 3 → 2（D1）。
- 事件状态机 `READY → PRODUCED` 一次性，重复生产拒绝（API 409，D9）。
- 成品质检三态：`QUALIFIED`（≥75）/ `HELD`（<75 留档）/ `BLOCKED`（一票否决），否决优先于分数（D9）。
- `auto_publish` 默认 `False`：系统只填草稿箱，发布是唯一人工动作（D5）。

## 5. 核心流程时序

**自动化主管线**（采集→聚类→素材入库→生产→质检已由后端真实实现；账号分配与入队之后仍由 demo 引擎模拟，逐步落地）：

```text
采集器(collector) ──信号──▶ 聚类(clustering，Jaccard≥0.35)──▶ 事件落库
事件 ──素材入库(asset_ingest：密度/授权标注落库)──▶ 形态判定（format_decision）
  ├─ 规则4 ──▶ 素材等待队列（事件 DEFERRED；补录素材自动重评估转 READY）
  └─ 规则1/2/3 ──▶ 生产引擎(producer：compose + 实时复跑判定，事务落库)──▶ 质检(quality_check)
        ├─ BLOCKED ──▶ 一票否决留档（禁止发布，待确认）
        ├─ HELD ──▶ 留档（<75，次日降级重试/人工捞回属发布配额概念，下一迭代）
        └─ QUALIFIED ──▶ 按垂类+剩余配额自动分配账号（待实现）──▶ 待发布队列
待发布队列 ──▶ RPA 填充头条草稿箱 ──▶ 人工点发布 ──▶ 数据回流（建议动作）
```

生产段细节（D9）：`produce_for_event` 对 READY 事件实时复跑形态判定 → `composer.compose`
产出 Draft（标题模板/正文三段/密度降序编排/封面）→ `check_quality` 三态判定 →
`db.transaction` 内原子完成 production 落库与事件状态转换；`produce_all` 批量容错。

**授权补录解锁路径**（demo 已验证交互）：素材 `pending → cleared` ⇒ 所属暂缓事件自动重评估形态 ⇒ 命中规则即转入生产候选。

## 6. 发布适配层设计

`app/publish/adapter.py` 定义协议，`app/publish/toutiao_draft.py` 是首个实现：

- **ContentPackage**：标准化内容包（title/body/format/video/cover/images/tags），构造时校验形态-素材匹配（视频形态必须有视频文件、图文必须有正文）。
- **PublishAdapter 协议**：`publish(package, account) -> PublishResult`，语义约定 `auto_publish=False` 只填草稿返回 `draft_ready`，`True` 直接发布返回 `published`。
- **登录态隔离**：每账号独立 Playwright 持久化 profile 目录，首次 `python -m app.publish.cli login` 扫码，之后复用；登录失效返回 `needs_login`。
- **选择器维护点**：头条后台 DOM 锚点集中在 `SELECTORS` 常量，平台改版只改这一处（当前为占位值，需首次真机校准）。
- **可测试性**：适配器构造函数注入 `browser_factory`，测试用 FakePage 替身验证交互顺序与开关语义，无需真实浏览器。

## 7. Demo 与后端的关系

- Demo（`demo/`）是**产品口径的可交互镜像**：`store.js` 中 `evalRule` 与后端 `format_decision.py` 同口径，供产品评审与流程验证；其模拟引擎演示目标态自动化流转。
- Demo 数据仅模拟（localStorage），不接后端；后端就绪后工作台切换到 FastAPI，视图层交互逻辑不变。
- 验证入口：`node demo/smoke.test.mjs`（14 项规则/质检/分配断言）。

## 8. 测试策略

| 层 | 方式 | 现有覆盖 |
| --- | --- | --- |
| 形态决策 | pytest 参数化用例 | `tests/test_format_decision.py`（11 例） |
| 发布适配器 | pytest + FakePage 替身 | `tests/test_publish_draft.py`（10 例：流程顺序、开关语义、登录短路、协议一致性） |
| DAO 层 | pytest + 临时 SQLite | `tests/test_daos.py`（11 例：幂等建表、事件/素材/成品往返、过滤排序） |
| 采集器 | pytest + httpx MockTransport | `tests/test_collector.py`（11 例：解析/归一化/容错，全离线） |
| 聚类 | pytest 参数化用例 | `tests/test_clustering.py`（15 例：阈值边界、并入/新建、D2 预判） |
| 素材入库 | pytest + 临时 SQLite | `tests/test_asset_ingest.py`（6 例：重评估、D2 不放行） |
| 内容生成器 | pytest 参数化用例 | `tests/test_composer.py`（12 例：标题模板/正文三段/编排封面/空素材边界） |
| 质检 | pytest 参数化用例 | `tests/test_quality_check.py`（15 例：否决独立触发/三态/75 边界/公式分项） |
| 生产编排 | pytest + 临时 SQLite | `tests/test_producer.py`（11 例：端到端落库/状态机拒绝/DEFER 防御/批量容错） |
| API | FastAPI TestClient + 依赖覆盖 | `tests/test_api.py`（19 例：collect/事件/补录/produce/productions/404/409/422） |
| Demo 业务逻辑 | node 冒烟脚本 | `demo/smoke.test.mjs`（14 项） |
| 门禁 | ruff（E/F/I，行宽 100）+ 全量 pytest，交付前必须通过 | 当前 121/121 |

## 9. 风险与边界

- **RPA 选择器漂移**：头条后台改版导致填稿失败 → 选择器集中管理 + 真机校准记录（见 `docs/context/RISKS.md`）。
- **平台风控**：高频自动化可能触发验证/封号 → 默认只填草稿 + 人工发布；操作间加随机延迟（待实现）；多账号注意 IP 关联。
- **合规底线不可降级**：素材授权为一票否决项，任何自动化不得绕过（D2）。
- **Demo 数据非真**：工作台数字均为模拟，不能作为运营决策依据。
